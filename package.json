{
  "private": true,
  "license": "UNLICENSED",
  "name": "captain",
  "version": "10.0.0",
  "description": "Docker menubar",
  "main": "src/backend/index.ts",
  "author": "Rick Wong <tangos_erasure0x@icloud.com>",
  "scripts": {
    "preinstall": "git submodule update --init --recursive",
    "clean": "rm -rf dist/*",
    "watch": "env NODE_ENV=development tsc-watch --onSuccess 'yarn dev'",
    "dev": "cp -R ./src/web/ ./dist/web/ && env NODE_ENV=development DEBUG=* electron --require ts-node/register --inspect=9991 ./",
    "start": "env NODE_ENV=production DEBUG=captain* electron --require ts-node/register ./",
    "prettier": "find ./ -type f \\( -iname \\*.js -o -iname \\*.json -o -iname \\*.ts -o -iname \\*.css \\) | grep -v logs/ | grep -v dist/ | grep -v node_modules/ | xargs prettier --write",
    "typecheck": "tsc --noEmit --pretty",
    "build": "yarn clean && yarn prettier && yarn typecheck",
    "dist": "yarn build && yarn dist:mac",
    "dist:mac": "export CSC_IDENTITY_AUTO_DISCOVERY=\"true\"; electron-builder --mac --x64 --arm64 -p always"
  },
  "engines": {
    "npm": "please-use-yarn",
    "yarn": ">= 1.19.1"
  },
  "prettier": {
    "arrowParens": "always",
    "endOfLine": "lf",
    "trailingComma": "all",
    "printWidth": 120
  },
  "devDependencies": {
    "@types/debug": "^4.1.7",
    "@types/electron": "^1.6.10",
    "@types/electron-positioner": "^4.1.1",
    "electron": "^19.0.4",
    "electron-builder": "^23.0.3",
    "prettier": "^2.7.0",
    "tsc-watch": "^5.0.3"
  },
  "dependencies": {
    "@electron/remote": "^2.0.8",
    "auto-launch-patched": "^5.0.2",
    "menubar": "^9.2.0",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.3"
  }
}
